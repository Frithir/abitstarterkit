/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ({

/***/ "./node_modules/lodash/_Symbol.js":
/*!****************************************!*\
  !*** ./node_modules/lodash/_Symbol.js ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var root = __webpack_require__(/*! ./_root */ "./node_modules/lodash/_root.js");
/** Built-in value references. */


var Symbol = root.Symbol;
module.exports = Symbol;

/***/ }),

/***/ "./node_modules/lodash/_baseGetTag.js":
/*!********************************************!*\
  !*** ./node_modules/lodash/_baseGetTag.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var Symbol = __webpack_require__(/*! ./_Symbol */ "./node_modules/lodash/_Symbol.js"),
    getRawTag = __webpack_require__(/*! ./_getRawTag */ "./node_modules/lodash/_getRawTag.js"),
    objectToString = __webpack_require__(/*! ./_objectToString */ "./node_modules/lodash/_objectToString.js");
/** `Object#toString` result references. */


var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';
/** Built-in value references. */

var symToStringTag = Symbol ? Symbol.toStringTag : undefined;
/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */

function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }

  return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
}

module.exports = baseGetTag;

/***/ }),

/***/ "./node_modules/lodash/_freeGlobal.js":
/*!********************************************!*\
  !*** ./node_modules/lodash/_freeGlobal.js ***!
  \********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

/* WEBPACK VAR INJECTION */(function(global) {/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;
module.exports = freeGlobal;
/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../webpack/buildin/global.js */ "./node_modules/webpack/buildin/global.js")))

/***/ }),

/***/ "./node_modules/lodash/_getRawTag.js":
/*!*******************************************!*\
  !*** ./node_modules/lodash/_getRawTag.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var Symbol = __webpack_require__(/*! ./_Symbol */ "./node_modules/lodash/_Symbol.js");
/** Used for built-in method references. */


var objectProto = Object.prototype;
/** Used to check objects for own properties. */

var hasOwnProperty = objectProto.hasOwnProperty;
/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */

var nativeObjectToString = objectProto.toString;
/** Built-in value references. */

var symToStringTag = Symbol ? Symbol.toStringTag : undefined;
/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */

function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);

  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }

  return result;
}

module.exports = getRawTag;

/***/ }),

/***/ "./node_modules/lodash/_objectToString.js":
/*!************************************************!*\
  !*** ./node_modules/lodash/_objectToString.js ***!
  \************************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/** Used for built-in method references. */
var objectProto = Object.prototype;
/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */

var nativeObjectToString = objectProto.toString;
/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */

function objectToString(value) {
  return nativeObjectToString.call(value);
}

module.exports = objectToString;

/***/ }),

/***/ "./node_modules/lodash/_root.js":
/*!**************************************!*\
  !*** ./node_modules/lodash/_root.js ***!
  \**************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var freeGlobal = __webpack_require__(/*! ./_freeGlobal */ "./node_modules/lodash/_freeGlobal.js");
/** Detect free variable `self`. */


var freeSelf = typeof self == 'object' && self && self.Object === Object && self;
/** Used as a reference to the global object. */

var root = freeGlobal || freeSelf || Function('return this')();
module.exports = root;

/***/ }),

/***/ "./node_modules/lodash/debounce.js":
/*!*****************************************!*\
  !*** ./node_modules/lodash/debounce.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(/*! ./isObject */ "./node_modules/lodash/isObject.js"),
    now = __webpack_require__(/*! ./now */ "./node_modules/lodash/now.js"),
    toNumber = __webpack_require__(/*! ./toNumber */ "./node_modules/lodash/toNumber.js");
/** Error message constants. */


var FUNC_ERROR_TEXT = 'Expected a function';
/* Built-in method references for those with the same name as other `lodash` methods. */

var nativeMax = Math.max,
    nativeMin = Math.min;
/**
 * Creates a debounced function that delays invoking `func` until after `wait`
 * milliseconds have elapsed since the last time the debounced function was
 * invoked. The debounced function comes with a `cancel` method to cancel
 * delayed `func` invocations and a `flush` method to immediately invoke them.
 * Provide `options` to indicate whether `func` should be invoked on the
 * leading and/or trailing edge of the `wait` timeout. The `func` is invoked
 * with the last arguments provided to the debounced function. Subsequent
 * calls to the debounced function return the result of the last `func`
 * invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the debounced function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.debounce` and `_.throttle`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to debounce.
 * @param {number} [wait=0] The number of milliseconds to delay.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=false]
 *  Specify invoking on the leading edge of the timeout.
 * @param {number} [options.maxWait]
 *  The maximum time `func` is allowed to be delayed before it's invoked.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new debounced function.
 * @example
 *
 * // Avoid costly calculations while the window size is in flux.
 * jQuery(window).on('resize', _.debounce(calculateLayout, 150));
 *
 * // Invoke `sendMail` when clicked, debouncing subsequent calls.
 * jQuery(element).on('click', _.debounce(sendMail, 300, {
 *   'leading': true,
 *   'trailing': false
 * }));
 *
 * // Ensure `batchLog` is invoked once after 1 second of debounced calls.
 * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
 * var source = new EventSource('/stream');
 * jQuery(source).on('message', debounced);
 *
 * // Cancel the trailing debounced invocation.
 * jQuery(window).on('popstate', debounced.cancel);
 */

function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }

  wait = toNumber(wait) || 0;

  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;
    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any `maxWait` timer.
    lastInvokeTime = time; // Start the timer for the trailing edge.

    timerId = setTimeout(timerExpired, wait); // Invoke the leading edge.

    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        timeWaiting = wait - timeSinceLastCall;
    return maxing ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime; // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the `maxWait` limit.

    return lastCallTime === undefined || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
  }

  function timerExpired() {
    var time = now();

    if (shouldInvoke(time)) {
      return trailingEdge(time);
    } // Restart the timer.


    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    timerId = undefined; // Only invoke if we have `lastArgs` which means `func` has been
    // debounced at least once.

    if (trailing && lastArgs) {
      return invokeFunc(time);
    }

    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }

    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);
    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }

      if (maxing) {
        // Handle invocations in a tight loop.
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }

    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }

    return result;
  }

  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}

module.exports = debounce;

/***/ }),

/***/ "./node_modules/lodash/isObject.js":
/*!*****************************************!*\
  !*** ./node_modules/lodash/isObject.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

module.exports = isObject;

/***/ }),

/***/ "./node_modules/lodash/isObjectLike.js":
/*!*********************************************!*\
  !*** ./node_modules/lodash/isObjectLike.js ***!
  \*********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

module.exports = isObjectLike;

/***/ }),

/***/ "./node_modules/lodash/isSymbol.js":
/*!*****************************************!*\
  !*** ./node_modules/lodash/isSymbol.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var baseGetTag = __webpack_require__(/*! ./_baseGetTag */ "./node_modules/lodash/_baseGetTag.js"),
    isObjectLike = __webpack_require__(/*! ./isObjectLike */ "./node_modules/lodash/isObjectLike.js");
/** `Object#toString` result references. */


var symbolTag = '[object Symbol]';
/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */

function isSymbol(value) {
  return typeof value == 'symbol' || isObjectLike(value) && baseGetTag(value) == symbolTag;
}

module.exports = isSymbol;

/***/ }),

/***/ "./node_modules/lodash/now.js":
/*!************************************!*\
  !*** ./node_modules/lodash/now.js ***!
  \************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var root = __webpack_require__(/*! ./_root */ "./node_modules/lodash/_root.js");
/**
 * Gets the timestamp of the number of milliseconds that have elapsed since
 * the Unix epoch (1 January 1970 00:00:00 UTC).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Date
 * @returns {number} Returns the timestamp.
 * @example
 *
 * _.defer(function(stamp) {
 *   console.log(_.now() - stamp);
 * }, _.now());
 * // => Logs the number of milliseconds it took for the deferred invocation.
 */


var now = function () {
  return root.Date.now();
};

module.exports = now;

/***/ }),

/***/ "./node_modules/lodash/throttle.js":
/*!*****************************************!*\
  !*** ./node_modules/lodash/throttle.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var debounce = __webpack_require__(/*! ./debounce */ "./node_modules/lodash/debounce.js"),
    isObject = __webpack_require__(/*! ./isObject */ "./node_modules/lodash/isObject.js");
/** Error message constants. */


var FUNC_ERROR_TEXT = 'Expected a function';
/**
 * Creates a throttled function that only invokes `func` at most once per
 * every `wait` milliseconds. The throttled function comes with a `cancel`
 * method to cancel delayed `func` invocations and a `flush` method to
 * immediately invoke them. Provide `options` to indicate whether `func`
 * should be invoked on the leading and/or trailing edge of the `wait`
 * timeout. The `func` is invoked with the last arguments provided to the
 * throttled function. Subsequent calls to the throttled function return the
 * result of the last `func` invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the throttled function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.throttle` and `_.debounce`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to throttle.
 * @param {number} [wait=0] The number of milliseconds to throttle invocations to.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=true]
 *  Specify invoking on the leading edge of the timeout.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new throttled function.
 * @example
 *
 * // Avoid excessively updating the position while scrolling.
 * jQuery(window).on('scroll', _.throttle(updatePosition, 100));
 *
 * // Invoke `renewToken` when the click event is fired, but not more than once every 5 minutes.
 * var throttled = _.throttle(renewToken, 300000, { 'trailing': false });
 * jQuery(element).on('click', throttled);
 *
 * // Cancel the trailing throttled invocation.
 * jQuery(window).on('popstate', throttled.cancel);
 */

function throttle(func, wait, options) {
  var leading = true,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }

  if (isObject(options)) {
    leading = 'leading' in options ? !!options.leading : leading;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  return debounce(func, wait, {
    'leading': leading,
    'maxWait': wait,
    'trailing': trailing
  });
}

module.exports = throttle;

/***/ }),

/***/ "./node_modules/lodash/toNumber.js":
/*!*****************************************!*\
  !*** ./node_modules/lodash/toNumber.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

var isObject = __webpack_require__(/*! ./isObject */ "./node_modules/lodash/isObject.js"),
    isSymbol = __webpack_require__(/*! ./isSymbol */ "./node_modules/lodash/isSymbol.js");
/** Used as references for various `Number` constants. */


var NAN = 0 / 0;
/** Used to match leading and trailing whitespace. */

var reTrim = /^\s+|\s+$/g;
/** Used to detect bad signed hexadecimal string values. */

var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
/** Used to detect binary string values. */

var reIsBinary = /^0b[01]+$/i;
/** Used to detect octal string values. */

var reIsOctal = /^0o[0-7]+$/i;
/** Built-in method references without a dependency on `root`. */

var freeParseInt = parseInt;
/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */

function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }

  if (isSymbol(value)) {
    return NAN;
  }

  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? other + '' : other;
  }

  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }

  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
}

module.exports = toNumber;

/***/ }),

/***/ "./node_modules/webpack/buildin/global.js":
/*!***********************************!*\
  !*** (webpack)/buildin/global.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

var g; // This works in non-strict mode

g = function () {
  return this;
}();

try {
  // This works if eval is allowed (see CSP)
  g = g || new Function("return this")();
} catch (e) {
  // This works if the window reference is available
  if (typeof window === "object") g = window;
} // g can still be undefined, but nothing to do about it...
// We return undefined, instead of nothing here, so it's
// easier to handle this case. if(!global) { ...}


module.exports = g;

/***/ }),

/***/ "./src/js/_home-nav.js":
/*!*****************************!*\
  !*** ./src/js/_home-nav.js ***!
  \*****************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* globals intersection-observer */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var header = document.querySelector('.home-header');
  if (!header) return;
  var homeTop = document.querySelector('.hometop');
  if (!homeTop) return;
  homeTop.style.height = header.offsetHeight * 3 + 'px';
  homeTop.style.position = 'absolute';
  var observer;

  var handleIntersect = function handleIntersect(entries) {
    entries.map(function (entry) {
      if (entry.isIntersecting) {
        header.classList.add('is-top');
        header.classList.remove('header');
      } else {
        header.classList.remove('is-top');
        header.classList.add('header');
      }
    });
  };

  observer = new IntersectionObserver(handleIntersect);
  observer.observe(homeTop);
});

/***/ }),

/***/ "./src/js/_lazyLoad.js":
/*!*****************************!*\
  !*** ./src/js/_lazyLoad.js ***!
  \*****************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

/* globals intersection-observer */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var args = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var options = {
    lazyParentClass: args.lazyParentClass || '.lazy-parent',
    lazyItemClass: args.lazyItemClass || '.lazy-child'
  };

  var lazyLoadParents = _toConsumableArray(document.querySelectorAll(options.lazyParentClass));

  var lazyLoad = new IntersectionObserver(function (entries) {
    entries.map(function (entry) {
      // check if observed entry is intersecting
      if (!entry.isIntersecting) return false; // target = intersected element

      var children = _toConsumableArray(entry.target.querySelectorAll(options.lazyItemClass));

      if (0 <= children.length) {
        children.forEach(function (img) {
          if (img.dataset.srcset) {
            // move data-srcset to srcset
            img.srcset = img.dataset.srcset;
            img.removeAttribute('data-srcset');
          }

          if (img.dataset.src) {
            // move data-src to src
            img.src = img.dataset.src;
            img.removeAttribute('data-src');
          } // background-image


          if (img.dataset.bgSrc) {
            var dummyImage = new Image();
            dummyImage.src = img.dataset.bgSrc;

            dummyImage.onload = function () {
              img.style.backgroundImage = "url(".concat(img.dataset.bgSrc, ")");
              img.removeAttribute('data-bg-src');
              img.classList.add('loaded');
            };
          } // wait for image to load and addClass to fade in


          img.onload = function () {
            return img.classList.add('loaded');
          };
        });
      }
    });
  });
  lazyLoadParents.forEach(function (item) {
    // add items to IntersectionObserver
    lazyLoad.observe(item);
  });
});

/***/ }),

/***/ "./src/js/_mobileNav.js":
/*!******************************!*\
  !*** ./src/js/_mobileNav.js ***!
  \******************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  $('.icon.burger').click(function () {
    $('body').css({
      overflow: 'hidden'
    });
    $('.mobile-nav-overlay').css({
      display: 'flex'
    }).animate({
      opacity: 1
    }, 300);
  });
  $('.mobile-nav-overlay .close').on('click', function () {
    $('.mobile-nav-overlay').animate({
      opacity: 0
    }, 300, function () {
      $('.mobile-nav-overlay').css({
        display: 'none'
      });
      $('body').css({
        overflow: 'auto'
      });
    });
  });
})(jQuery);

/***/ }),

/***/ "./src/js/_navScroll.js":
/*!******************************!*\
  !*** ./src/js/_navScroll.js ***!
  \******************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var header = document.querySelector('.header');
  if (!header) return;

  function add_class_on_scroll() {
    header.classList.add('fade-in');
  }

  function remove_class_on_scroll() {
    header.classList.remove('fade-in');
  }

  window.addEventListener('scroll', function () {
    var scrollpos = window.scrollY;

    if (scrollpos > 1) {
      add_class_on_scroll();
    } else {
      remove_class_on_scroll();
    }
  });

  window.onbeforeunload = function () {
    window.scrollTo(0, 0);
  };
});

/***/ }),

/***/ "./src/js/_popup.js":
/*!**************************!*\
  !*** ./src/js/_popup.js ***!
  \**************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

var popupClassName = 'popup-content';
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var popupButtons = _toConsumableArray(document.querySelectorAll('.popup'));

  if (!popupButtons) return;

  var addListeners = function addListeners(popupElement) {
    window.addEventListener('keydown', removePopup);
    popupElement.addEventListener('click', removePopup);
  };

  var removePopup = function removePopup(e) {
    if ('click' === e.type && !e.target.classList.contains(popupClassName)) return;
    if ('keydown' === e.type && 'Escape' !== e.key) return;
    var popupElement = document.querySelector(".".concat(popupClassName));
    popupElement.remove();
    window.removeEventListener('keydown', removePopup);
    document.body.classList.remove('popup-open');
  };

  var openPopup = function openPopup(e) {
    e.preventDefault();
    var html = "\n      <div class=\"popup-content container\">\n        <img src=\"".concat(this.href, "\" />\n      </div>\n    ");
    var popup = document.createElement('div');
    popup.classList.add(popupClassName);
    popup.innerHTML = html;
    document.body.appendChild(popup);
    document.body.classList.add('popup-open');
    addListeners(popup);
  };

  popupButtons.forEach(function (btn) {
    return btn.addEventListener('click', openPopup);
  });
});

/***/ }),

/***/ "./src/js/_popupVideo.js":
/*!*******************************!*\
  !*** ./src/js/_popupVideo.js ***!
  \*******************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance"); }

function _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === "[object Arguments]") return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }

/* harmony default export */ __webpack_exports__["default"] = (function () {
  var popupClassName = 'video-popup',
      popupButtons = _toConsumableArray(document.querySelectorAll('.video-opener'));

  if (!popupButtons) return;

  var addListeners = function addListeners(popupElement) {
    window.addEventListener('keydown', removeVideo);
    popupElement.addEventListener('click', removeVideo);
  },
      removeVideo = function removeVideo(e) {
    if (e.type === 'click' && !e.target.classList.contains(popupClassName) && !e.target.classList.contains('close')) return;
    if (e.type === 'keydown' && e.key !== 'Escape') return;
    var popupElement = document.querySelector(".".concat(popupClassName));
    popupElement.classList.remove('active');
    setTimeout(function () {
      popupElement.remove();
      window.removeEventListener('keydown', removeVideo);
    }, 300);
  },
      openPopup = function openPopup(e) {
    e.preventDefault();
    var html = "\n      <div class=\"close\"></div>\n      <video controls autoplay>\n        <source src=\"".concat(e.target.attributes.href.value, "\" type=\"video/mp4\">\n      </video>\n    "),
        popup = document.createElement('div');
    popup.classList.add(popupClassName);
    popup.innerHTML = html;
    document.body.appendChild(popup);
    setTimeout(function () {
      popup.classList.add('active');
    }, 300);
    addListeners(popup);
  };

  popupButtons.forEach(function (btn) {
    return btn.addEventListener('click', openPopup);
  });
});

/***/ }),

/***/ "./src/js/_quantity.js":
/*!*****************************!*\
  !*** ./src/js/_quantity.js ***!
  \*****************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  $('.plusminuslabel').click(function () {
    var $button = $(this);
    var oldValue = $button.parent().find('.input-text.qty').val();
    var newVal;

    if ($button.hasClass('pluslabel')) {
      newVal = parseFloat(oldValue) + 1;
    } else {
      // Don't allow decrementing below zero
      if (0 < oldValue) {
        newVal = parseFloat(oldValue) - 1;
      } else {
        newVal = 0;
      }
    }

    $button.parent().find('.input-text.qty').val(newVal);
  });
})(jQuery);

/***/ }),

/***/ "./src/js/_tab.js":
/*!************************!*\
  !*** ./src/js/_tab.js ***!
  \************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  var btnKey = '';
  $('.open-filters').click(function (e) {
    e.preventDefault();
    $(this).toggleClass('openAllFilters');
    $('.all-filters').toggleClass('open');
  });
  $('.tab-btn').click(function (e) {
    e.preventDefault();
    btnKey = $(this).attr('data-filter');
    $('.tab-content').removeClass('openTab');
    $('.tab-btn').removeClass('openTabBtn');
    $(this).addClass('openTabBtn');
    $('.tab-content[data-key=' + btnKey + ']').addClass('openTab');
  });
})(jQuery);

/***/ }),

/***/ "./src/js/app.js":
/*!***********************!*\
  !*** ./src/js/app.js ***!
  \***********************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _components_flickity_sliders__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./components/_flickity-sliders */ "./src/js/components/_flickity-sliders.js");
/* harmony import */ var _components_featured_slider__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./components/_featured-slider */ "./src/js/components/_featured-slider.js");
/* harmony import */ var _components_InstagramFeed_slider__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./components/_InstagramFeed-slider */ "./src/js/components/_InstagramFeed-slider.js");
/* harmony import */ var _components_Gallery__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./components/_Gallery */ "./src/js/components/_Gallery.js");
/* harmony import */ var _lazyLoad__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./_lazyLoad */ "./src/js/_lazyLoad.js");
/* harmony import */ var _home_nav__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./_home-nav */ "./src/js/_home-nav.js");
/* harmony import */ var _popup__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./_popup */ "./src/js/_popup.js");
/* harmony import */ var _navScroll__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./_navScroll */ "./src/js/_navScroll.js");
/* harmony import */ var _components_storeLocator__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./components/_storeLocator */ "./src/js/components/_storeLocator.js");
/* harmony import */ var _popupVideo__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./_popupVideo */ "./src/js/_popupVideo.js");
/* harmony import */ var _mobileNav__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./_mobileNav */ "./src/js/_mobileNav.js");
/* harmony import */ var _mobileNav__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(_mobileNav__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var _quantity__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./_quantity */ "./src/js/_quantity.js");
/* harmony import */ var _quantity__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(_quantity__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var _tab__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./_tab */ "./src/js/_tab.js");
/* harmony import */ var _tab__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(_tab__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var _components_accordion__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./components/_accordion */ "./src/js/components/_accordion.js");
/* harmony import */ var _components_accordion__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(_components_accordion__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var _components_search_dropdown__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./components/_search-dropdown */ "./src/js/components/_search-dropdown.js");
/* harmony import */ var _components_search_dropdown__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(_components_search_dropdown__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var _components_woo_gallery__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./components/_woo-gallery */ "./src/js/components/_woo-gallery.js");
/* harmony import */ var _components_woo_gallery__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(_components_woo_gallery__WEBPACK_IMPORTED_MODULE_15__);
/* globals jQuery */

















function init() {
  jQuery('input[type="submit"]').each(function (i, e) {
    var attrStr = '',
        text = '';
    jQuery.each(e.attributes, function (i, elem) {
      if ('value' === elem.name) text = elem.value;else attrStr += ' ' + elem.name + '="' + elem.value + '"';
    });
    jQuery(e).replaceWith('<button' + attrStr + '>' + text + '</button>');
  });
  Object(_components_storeLocator__WEBPACK_IMPORTED_MODULE_8__["default"])();
  Object(_components_flickity_sliders__WEBPACK_IMPORTED_MODULE_0__["default"])();
  Object(_components_featured_slider__WEBPACK_IMPORTED_MODULE_1__["default"])();
  Object(_components_InstagramFeed_slider__WEBPACK_IMPORTED_MODULE_2__["default"])();
  Object(_lazyLoad__WEBPACK_IMPORTED_MODULE_4__["default"])();
  Object(_home_nav__WEBPACK_IMPORTED_MODULE_5__["default"])();
  Object(_popup__WEBPACK_IMPORTED_MODULE_6__["default"])();
  Object(_navScroll__WEBPACK_IMPORTED_MODULE_7__["default"])();
  Object(_components_Gallery__WEBPACK_IMPORTED_MODULE_3__["default"])();
  Object(_popupVideo__WEBPACK_IMPORTED_MODULE_9__["default"])();
}

init();

/***/ }),

/***/ "./src/js/components/_Gallery.js":
/*!***************************************!*\
  !*** ./src/js/components/_Gallery.js ***!
  \***************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* globals jQuery PhotoSwipe PhotoSwipeUI_Default masonry-layout  */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var container = [],
      init = function init() {
    if (!jQuery('.madi-brides .grid').length) return;
    initGrid();
    jQuery('.madi-brides .grid').animate({
      opacity: 1
    }, 3); // jQuery(window).load(() => {
    //   initGrid()
    //   jQuery('.madi-brides .grid').animate({ opacity: 1 }, 300)
    // })

    getGalleryItems();
    bindClickEvent();
  },
      initGrid = function initGrid() {
    new Masonry(document.querySelector('.grid'), {
      columnWidth: '.grid-sizer',
      itemSelector: '.grid-item',
      percentPosition: true,
      gutter: 10
    });
  },
      getGalleryItems = function getGalleryItems() {
    jQuery('.grid').find('figure').each(function () {
      var $link = jQuery(this).find('img'),
          item = {
        src: $link.data('hd'),
        w: $link.data('w'),
        h: $link.data('h'),
        title: $link.data('title')
      };
      container.push(item);
    });
  },
      bindClickEvent = function bindClickEvent() {
    jQuery('.grid-item').on('click', function (e) {
      e.preventDefault();
      var $pswp = jQuery('.pswp')[0],
          options = {
        index: jQuery(e.currentTarget).index() - 1,
        bgOpacity: 0.85,
        showHideOpacity: true
      },
          gallery = new PhotoSwipe($pswp, PhotoSwipeUI_Default, container, options);
      gallery.init();
    });
  };

  init();
});

/***/ }),

/***/ "./src/js/components/_InstagramFeed-slider.js":
/*!****************************************************!*\
  !*** ./src/js/components/_InstagramFeed-slider.js ***!
  \****************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* globals jQuery flickity-imagesloaded  */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var instagramFeed = document.querySelector('.InstagramFeed--row');
  if (!instagramFeed) return;
  new Flickity(instagramFeed, {
    setGallerySize: true,
    wrapAround: true,
    pageDots: false,
    prevNextButtons: false,
    imagesLoaded: true,
    freeScrollFriction: 10,
    selectedAttraction: 0.0001,
    autoPlay: 10000,
    friction: 0.1,
    cellAlign: 'left'
  });
});

/***/ }),

/***/ "./src/js/components/_accordion.js":
/*!*****************************************!*\
  !*** ./src/js/components/_accordion.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  $('.Accordion > .Accordion--item > h4').click(function () {
    if ($(this).parent().hasClass('active')) {
      $(this).parent().removeClass('active');
    } else {
      $('.Accordion--item').removeClass('active');
      $(this).parent().addClass('active');
    }

    setTimeout(function () {
      $(document.body).trigger('sticky_kit:recalc');
    }, 300);
  });
})(jQuery);

/***/ }),

/***/ "./src/js/components/_featured-slider.js":
/*!***********************************************!*\
  !*** ./src/js/components/_featured-slider.js ***!
  \***********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* globals jQuery flickity-imagesloaded  */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  var gallery = document.querySelector('.gallery');
  if (!gallery) return;
  new Flickity(gallery, {
    wrapAround: true,
    autoPlay: 4000,
    imagesLoaded: true,
    pageDots: false //cellAlign: 'left',

  });
});

/***/ }),

/***/ "./src/js/components/_flickity-sliders.js":
/*!************************************************!*\
  !*** ./src/js/components/_flickity-sliders.js ***!
  \************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* globals jQuery flickity-imagesloaded  */
/* harmony default export */ __webpack_exports__["default"] = (function () {
  (function () {
    var fullwidthGallery = document.querySelector('.featured-slider');

    if (!fullwidthGallery) {
      return;
    }

    var slides = fullwidthGallery.querySelectorAll('.overview-block-product');

    if (fullwidthGallery && 1 < slides.length) {
      new Flickity(fullwidthGallery, {
        setGallerySize: true,
        wrapAround: true,
        pageDots: false,
        prevNextButtons: true,
        autoPlay: 34000,
        imagesLoaded: true,
        cellAlign: 'left',
        arrowShape: {
          x0: 10,
          x1: 60,
          y1: 50,
          x2: 60,
          y2: 45,
          x3: 15
        }
      });
    } else {
      fullwidthGallery.classList.remove('flickity');
    }
  })();
});

/***/ }),

/***/ "./src/js/components/_search-dropdown.js":
/*!***********************************************!*\
  !*** ./src/js/components/_search-dropdown.js ***!
  \***********************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  $('.search-link').click(function (e) {
    e.preventDefault();

    if ($('.search-overlay').hasClass('active')) {
      $('.search-overlay').removeClass('active');
    } else {
      $('body').css({
        'overflow-y': 'hidden'
      });
      $('.search-overlay').addClass('active').find('.site-search').focus();
      $('.search-overlay').animate({
        opacity: 1
      }, 300, 'linear');
    }
  });
  $('.search-overlay').find('.close').on('click', function (e) {
    e.stopPropagation();
    $('.search-overlay').animate({
      opacity: 0
    }, 300, 'linear', function () {
      $('.search-overlay').removeClass('active');
    });
    $('body').css({
      'overflow-y': 'auto'
    });
  });
  $('.search-overlay').on('click', function (e) {
    e.stopPropagation();

    if ($(e.target).hasClass('search-overlay')) {
      $('.search-overlay').find('.close').trigger('click');
    }
  });
})(jQuery);

/***/ }),

/***/ "./src/js/components/_storeLocator.js":
/*!********************************************!*\
  !*** ./src/js/components/_storeLocator.js ***!
  \********************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var lodash_throttle__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! lodash/throttle */ "./node_modules/lodash/throttle.js");
/* harmony import */ var lodash_throttle__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(lodash_throttle__WEBPACK_IMPORTED_MODULE_0__);
/* globals google jQuery masonry-layout lodash/throttle */
 // import Masonry from 'masonry-layout';

var storeLocator = function storeLocator() {
  var map = null,
      markers = [],
      autocomplete,
      countryRestrict = {
    country: ['au', 'us']
  },
      zoomed = false;
  var mapElement = document.querySelector('.store-locator--map'),
      markerElements = Array.from(document.querySelectorAll('.store-locator--map .store-locator--marker')),
      //resultsElement = document.querySelector('.store-locator--results'),
  autocompleteElement = document.querySelector('.store-locator--inputs--location input');
  if (!mapElement) return false; // get options passed from php

  var options = JSON.parse(mapElement.dataset.options),
      args = {
    center: options.center || {
      lat: 0,
      lng: 25
    },
    styles: [{
      featureType: 'administrative',
      elementType: 'all',
      stylers: [{
        visibility: 'on'
      }, {
        saturation: -100
      }, {
        lightness: 20
      }]
    }, {
      featureType: 'road',
      elementType: 'all',
      stylers: [{
        visibility: 'on'
      }, {
        saturation: -100
      }, {
        lightness: 40
      }]
    }, {
      featureType: 'water',
      elementType: 'all',
      stylers: [{
        visibility: 'on'
      }, {
        saturation: -10
      }, {
        lightness: 30
      }]
    }, {
      featureType: 'landscape.man_made',
      elementType: 'all',
      stylers: [{
        visibility: 'simplified'
      }, {
        saturation: -60
      }, {
        lightness: 10
      }]
    }, {
      featureType: 'landscape.natural',
      elementType: 'all',
      stylers: [{
        visibility: 'simplified'
      }, {
        saturation: -60
      }, {
        lightness: 60
      }]
    }, {
      featureType: 'poi',
      elementType: 'all',
      stylers: [{
        visibility: 'off'
      }, {
        saturation: -100
      }, {
        lightness: 60
      }]
    }, {
      featureType: 'transit',
      elementType: 'all',
      stylers: [{
        visibility: 'off'
      }, {
        saturation: -100
      }, {
        lightness: 60
      }]
    }],
    zoom: 2,
    scrollwheel: options.scrollwheel === undefined ? true : options.scrollwheel,
    draggable: options.draggable || false,
    disableDefaultUI: options.disableDefaultUI || false
  },
      initMap = function initMap() {
    map = new google.maps.Map(mapElement, args);
    markerElements.forEach(function (markerEl) {
      return addMarker(markerEl, map);
    });
    initMasonryGrid();
    initAutocomplete();
    addListeners(); // Try HTML5 geolocation.

    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(function (position) {
        var pos = {
          lat: position.coords.latitude,
          lng: position.coords.longitude
        };
        map.panTo(pos);
        map.setZoom(5);
        zoomed = true;
      }, function (err) {
        return console.log('Geolocation failed', err);
      });
    } else {
      // Browser doesn't support Geolocation
      console.log("Browser doesn't support Geolocation");
    }
  },
      initMasonryGrid = function initMasonryGrid() {
    if (document.getElementsByClassName('locations-grid')) {
      (function () {
        var elements = document.getElementsByClassName('locations-grid');

        var _loop = function _loop(i) {
          var width = jQuery(window).width();
          jQuery(window).load(function () {
            new Masonry(elements[i], {
              columnWidth: '.locations-grid-sizer',
              itemSelector: '.locations-grid-item',
              percentPosition: true,
              gutter: 830 < width ? 50 : 10
            });
            jQuery('.locations-grid').animate({
              opacity: 1
            }, 300);
          });
        };

        for (var i = 0; i < elements.length; i++) {
          _loop(i);
        }
      })();
    }
  },
      initAutocomplete = function initAutocomplete() {
    autocomplete = new google.maps.places.Autocomplete(
    /** @type {!HTMLInputElement} */
    autocompleteElement, {
      types: ['(regions)'],
      componentRestrictions: countryRestrict
    });
  },
      addListeners = function addListeners() {
    map.addListener('bounds_changed', lodash_throttle__WEBPACK_IMPORTED_MODULE_0___default()(updateVisible, 200));
    autocomplete.addListener('place_changed', onPlaceChanged);
  },
      onPlaceChanged = function onPlaceChanged() {
    var place = autocomplete.getPlace();
    zoomed = false;

    if (place && place.geometry) {
      map.panTo(place.geometry.location);
      map.setZoom(args.zoom);
    } else {
      autocompleteElement.placeholder = 'Enter a city';
    }
  },
      updateVisible = function updateVisible() {
    var bounds = map.getBounds();
    markers.forEach(function (marker) {
      var isVisible = bounds.contains(marker.getPosition());
      marker.setVisible(isVisible);
      if (!isVisible) marker.infowindow.close();
    });

    if ('undefined' !== autocomplete.getPlace() && !zoomed) {
      //console.log(autocomplete.getPlace().geometry.viewport)
      map.fitBounds(autocomplete.getPlace().geometry.viewport);
      zoomed = true;
    } // renderResults()

  },
      // renderResults = () => {
  //   let results = []
  //   markers.forEach(marker => {
  //     console.log(marker)
  //     if (!marker.visible) return
  //     const markerHTML = `<div class="store-locator--results--item">${
  //       marker.infowindow.content
  //     }</div>`
  //     results.push(markerHTML)
  //   })
  //   resultsElement.innerHTML = results.join('')
  // },
  addMarker = function addMarker(markerEl, map) {
    var center = JSON.parse(markerEl.dataset.center),
        latlng = new google.maps.LatLng(center.lat, center.lng),
        icon = {
      url: "".concat(window.wpGlobal.templateUrl, "/images/marker.svg"),
      scaledSize: new google.maps.Size(60 / 2, 54 / 2),
      anchor: new google.maps.Point(60 / 2 / 2, 54 / 2 / 2)
    },
        marker = new google.maps.Marker({
      position: latlng,
      map: map,
      icon: icon
    });
    markers.push(marker);

    if (markerEl.innerHTML) {
      marker.infowindow = new google.maps.InfoWindow({
        content: markerEl.innerHTML
      });
      google.maps.event.addListener(marker, 'click', function () {
        marker.infowindow.open(map, marker);
      });
    }
  };

  initMap();
};

/* harmony default export */ __webpack_exports__["default"] = (storeLocator);

/***/ }),

/***/ "./src/js/components/_woo-gallery.js":
/*!*******************************************!*\
  !*** ./src/js/components/_woo-gallery.js ***!
  \*******************************************/
/*! no static exports found */
/***/ (function(module, exports) {

/* globals jQuery */
(function ($) {
  var lastActiveItem = false,
      activeItem = false;

  var init = function init() {
    if (!$('.single-product').length) return;
    initStickyContent();
    initThumbnailClick();
    setActiveItem();
    $('.woo-gallery-item:not(.video)').each(function (i, e) {
      $(e).zoom({
        on: 'click'
      }).css({
        cursor: 'zoom-in'
      });
    });
  },
      initStickyContent = function initStickyContent() {
    var initStickyKit = function initStickyKit() {
      if (830 <= $(window).width()) {
        $('.sticky-item').stick_in_parent({
          offset_top: 80
        });
      } else {
        $('.sticky-item[data-responsive!="desktop-only"]').stick_in_parent({
          offset_top: 75
        });
      }
    };

    $(window).on('resize', function () {
      $('.sticky-item').trigger('sticky_kit:detach');
      initStickyKit();
    });
    initStickyKit();
  },
      initThumbnailClick = function initThumbnailClick() {
    $('.woo-gallery-thumbnail').on('click', function (e) {
      e.preventDefault();
      var $element = $('.woo-gallery-item:nth-child(' + ($(e.currentTarget).index() + 1) + ')');
      scrollToElement($element);
    });
  },
      scrollToElement = function scrollToElement($element) {
    $([document.documentElement, document.body]).stop().animate({
      scrollTop: $element.offset().top - $('header').height() / 2 + $element.height() / 2 - $(window).height() / 2
    }, 1000);
  },
      setActiveItem = function setActiveItem() {
    var trashHold = $(window).height() / 2;
    $('.woo-gallery-sidebar .woo-gallery-thumbnail:first-of-type, .woo-gallery-items .woo-gallery-item:first-of-type').addClass('active');
    $(window).on('scroll', function (e) {
      trashHold = e.currentTarget.scrollY + $(window).height() / 2;
      handleVideoPlay(trashHold);
      changeActiveItem(trashHold);
    });
    handleVideoPlay(trashHold);
    changeActiveItem(trashHold);
  },
      handleVideoPlay = function handleVideoPlay(trashHold) {
    $('.woo-gallery-item.video').each(function (i, e) {
      var $video = $(e).find('video').get(0);

      if ($(e).offset().top + $(e).height() < trashHold || $(e).offset().top > trashHold) {
        $video.pause();
      } else {
        $video.play();
      }
    });
  },
      changeActiveItem = function changeActiveItem(trashHold) {
    $('.woo-gallery-item').each(function (i, e) {
      if ($(e).offset().top < trashHold) activeItem = e;
    });

    if (activeItem !== lastActiveItem) {
      $('.woo-gallery-item, .woo-gallery-thumbnail').removeClass('active');
      $(activeItem).addClass('active');
      $('.woo-gallery-thumbnail:nth-child(' + ($(activeItem).index() + 1) + ')').addClass('active');
      lastActiveItem = activeItem;
    }
  };

  init();
})(jQuery);

/***/ }),

/***/ "./src/scss/app.scss":
/*!***************************!*\
  !*** ./src/scss/app.scss ***!
  \***************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

// extracted by mini-css-extract-plugin

/***/ }),

/***/ 0:
/*!*************************************************!*\
  !*** multi ./src/js/app.js ./src/scss/app.scss ***!
  \*************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

__webpack_require__(/*! ./src/js/app.js */"./src/js/app.js");
module.exports = __webpack_require__(/*! ./src/scss/app.scss */"./src/scss/app.scss");


/***/ })

/******/ });
//# sourceMappingURL=app.min.js.map